<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Page Title</title>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link type="text/css" rel="stylesheet" href="materialize/css/materialize.min.css" media="screen,projection" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
</head>
<body>
    <br><br>
    <div class="row">
        <div class="col s6 offset-s3">
            <div class="card blue lighten-5">
                <br>
                <div class="card-content">
                    <span class="card-title center">Welcome to the Great Number Game!</span>
                    <p class="center">I am thinking of a number between 1 and 100</p>
                    <br>
                    <div class="row">
                        <% if(context.result == 'Greater') { %>
                            <div class="col s6 offset-s3">
                                <div class="card red">
                                    <br><br><div class="card-content">
                                        <span class="card-title center">Too High!</span>
                                    </div><br><br>
                                </div>
                            </div>
                        <% } %>
                        <% if(context.result == 'Less') { %>
                            <div class="col s6 offset-s3">
                                <div class="card red">
                                    <br><br><div class="card-content">
                                        <span class="card-title center">Too Low!</span>
                                    </div><br><br>
                                </div>
                            </div>
                        <% } %>
                        <% if(context.result != 'Win') { %>
                            <form class="col s4 center offset-s4" action="/" method="POST">
                                <input type="number" placeholder="Take A Guess!" name="guess">
                                <button class="waves-effect waves-light btn " type="submit">Button</button>
                            </form>
                        <% } %>
                        <% if(context.result == 'Win') { %>
                            <div class="col s6 offset-s3">
                                <div class="card green darken-4">
                                    <br><br><div class="card-content">
                                        <span class="card-title center">You Win!!!</span>
                                    </div><br><br>
                                </div>
                            </div>
                            <form class="col s4 center offset-s4" action="/" method="POST">
                                <input type="hidden" value="NewGame" name="NewGame">
                                <button class="waves-effect waves-light btn " type="submit">Play Again?</button>
                            </form>
                        <% } %>
                    </div>
                </div>
                </div>
            </div>
        </div>
    <script type="text/javascript" src="https://code.jquery.com/jquery-3.2.1.min.js"></script>
    <script type="text/javascript" src="materialize/js/materialize.min.js"></script>
</body>
</html>